From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Deepa <deepa@calyx.org>
Date: Mon, 24 Aug 2026 23:39:01 +0000
Subject: search_engines-add-DuckDuckGo-as-selectable-and-defa

---
 .../template_url_prepopulate_data.cc               | 14 +++++++-------
 1 file changed, 7 insertions(+), 7 deletions(-)

diff --git a/components/search_engines/template_url_prepopulate_data.cc b/components/search_engines/template_url_prepopulate_data.cc
index 6a05437fabd4e..4a35e5175c79b 100644
--- a/components/search_engines/template_url_prepopulate_data.cc
+++ b/components/search_engines/template_url_prepopulate_data.cc
@@ -193,12 +193,12 @@ std::vector<std::unique_ptr<TemplateURLData>> GetPrepopulatedEnginesChromium(
 
 std::vector<std::unique_ptr<TemplateURLData>> GetPrepopulatedEngines(
     PrefService& prefs,
-    const std::vector<const PrepopulatedEngine*>&
+    const std::vector<raw_ptr<const PrepopulatedEngine>>&
         regional_prepopulated_engines) {
   std::vector<std::unique_ptr<TemplateURLData>> t_urls =
-    GetPrepopulatedEnginesChromium(prefs, regional_prepopulated_engines);
-  t_urls.push_back(TemplateURLDataFromPrepopulatedEngine(googleen));
-  t_urls.push_back(TemplateURLDataFromPrepopulatedEngine(duckduckgo_light));
+      GetPrepopulatedEnginesChromium(prefs, regional_prepopulated_engines);
+  t_urls.push_back(PrepopulatedEngineToTemplateURLData(&googleen));
+  t_urls.push_back(PrepopulatedEngineToTemplateURLData(&duckduckgo_light));
   return t_urls;
 }
 
@@ -228,8 +228,8 @@ std::vector<std::unique_ptr<TemplateURLData>> GetLocalPrepopulatedEngines(
                             country_id, prefs,
                             regional_capabilities::SearchEngineListType::kTopN),
                         &PrepopulatedEngineToTemplateURLData);
-  t_urls.push_back(TemplateURLDataFromPrepopulatedEngine(googleen));
-  t_urls.push_back(TemplateURLDataFromPrepopulatedEngine(duckduckgo_light));
+  t_urls.push_back(PrepopulatedEngineToTemplateURLData(&googleen));
+  t_urls.push_back(PrepopulatedEngineToTemplateURLData(&duckduckgo_light));
   return t_urls;
 }
 
@@ -296,7 +296,7 @@ std::unique_ptr<TemplateURLData> GetPrepopulatedFallbackSearch(
     const std::vector<raw_ptr<const PrepopulatedEngine>>&
         regional_prepopulated_engines) {
   return FindPrepopulatedEngineInternal(prefs, regional_prepopulated_engines,
-                                        google.id,
+                                        duckduckgo.id,
                                         /*use_first_as_fallback=*/true);
 }
 
-- 
2.39.5

